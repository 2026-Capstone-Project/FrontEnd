# [2026년 03월 23일] 모바일 사용자 경험(UX) 최적화 및 브랜드 에셋 고도화

오늘 팀 "Calio"는 서비스의 완성도를 높이기 위한 세부적인 UI/UX 개선과 브랜드 아이덴티티 강화 작업을 진행했습니다. 특히 모바일 환경에서 발생하던 스타일 이슈를 해결하고, 사용자 인터페이스의 일관성을 확보하는 데 집중했습니다. 메인 페이지의 레이아웃 정렬과 헤더의 반응형 동작을 보완하여 더욱 견고한 프론트엔드 환경을 구축했습니다.

---

## 🌿 Branch: chore/#54

### 1. 모바일 환경에서의 버튼 시인성 개선
**yujin5959** 개발자는 모바일 브라우저의 기본 테마나 시스템 설정에 따라 로그인 버튼의 글자 색상이 의도치 않게 변경되는 이슈를 해결했습니다. 기존 `color: inherit` 설정을 `color: black`으로 명시적으로 고정하여, 어떠한 환경에서도 텍스트가 명확하게 노출되도록 조치했습니다.

```typescript
// src/features/Auth/components/LoginCard/LoginCard.style.ts 변경
export const ButtonText = styled.span`
  flex: 1;
  text-align: center;
-  color: inherit;
+  color: black;
   text-decoration: none;
   &:visited,
   &:active,
   &:link {
-    color: inherit;
+    color: black;
     text-decoration: none;
   }
 `
```

### 2. 브랜드 에셋 반영 및 메타 데이터 업데이트
서비스의 첫인상을 결정짓는 favicon을 기본 Vite 로고에서 Calio 전용 로고로 교체했습니다. 이는 브랜드 아이덴티티를 확립하는 중요한 작업으로, `index.html`의 메타 태그 수정과 함께 신규 SVG 에셋이 추가되었습니다.

```html
<!-- index.html 변경 -->
 <head>
   <meta charset="UTF-8" />
   <meta name="Calio" content="말하는 대로 움직이는 일정" />
-  <link rel="icon" type="image/svg+xml" href="/vite.svg" />
+  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
   <meta name="viewport" content="width=device-width, initial-scale=1.0" />
   <title>말하는 대로 움직이는 일정, Calio</title>
 </head>
```

### 3. 모바일 네비게이션 및 레이아웃 안정화
**yujin5959** 개발자는 모바일 환경에서 네비게이션 바(Navbar)가 깨지는 현상을 수정하고, 사용자 편의성을 위해 헤더를 상단에 고정(`sticky`)했습니다. 또한, `HomePage` 내 카드 헤더의 정렬을 맞추고 태그 스타일을 조정하여 시각적인 균형을 잡았습니다.

특히 `Header.tsx`에서는 모바일 메뉴의 개폐 상태를 관리하기 위한 `isOpen` 상태값을 추가하여, 향후 구현될 모바일 전용 메뉴 대응을 위한 기반을 마련했습니다.

```typescript
// src/shared/ui/Header/Header.styles.ts 변경
export const HeaderWrapper = styled.header`
   width: 100%;
   background-color: ${theme.colors.white};
   border-bottom: 1px solid ${theme.colors.lightGray};
-  box-shadow: 0 0 15px 0 rgba(0, 0, 0, 0.15);
+  box-shadow: 0 0 15px 0 rgba(0, 0, 0, 0.05);
   height: 72px;
+  position: sticky;
+  top: 0;
+  z-index: 1000;
 `

// src/shared/ui/Header/Header.tsx 변경
export default function Header() {
+  const [isOpen, setIsOpen] = useState(false)
+
+  const toggleMenu = () => setIsOpen((prev) => !prev)
+  const closeMenu = () => setIsOpen(false)
+
   return (
     <S.HeaderWrapper>
       <S.Inner>
-        <S.LogoLink to="/">
+        <S.LogoLink to="/" onClick={closeMenu}>
           <S.Logo src={logo} alt="Cali/o Logo" />
```

### 4. 메인 페이지 UI 디테일 수정
메인 페이지의 `CardHeader`에 `align-items: center`를 추가하여 요소 간의 수직 정렬을 맞추고, `Tag` 컴포넌트의 여백과 폰트 스타일을 조정하여 가독성을 높였습니다.

```typescript
// src/pages/main/HomePage/HomePage.styles.ts 변경
 export const CardHeader = styled.div`
   display: flex;
   justify-content: space-between;
+  align-items: center;
+  margin-bottom: 12px;
 `
```

---

**시니어 개발자 총평:**
오늘 작업은 서비스의 디테일을 완성하는 데 큰 의미가 있었습니다. 특히 모바일 환경에서의 스타일 상속 이슈는 자칫 놓치기 쉬운 부분인데, 이를 명시적으로 해결함으로써 사용자에게 일관된 UI를 제공할 수 있게 되었습니다. 헤더의 `sticky` 처리와 `z-index` 설정은 향후 복잡해질 레이아웃 구조에서 네비게이션의 우선순위를 보장하는 적절한 조치였습니다. 앞으로도 이러한 세밀한 최적화를 통해 서비스의 퀄리티를 지속적으로 높여나가겠습니다.