**[2026년 02월 01일] 자동 개발 일지 워크플로 안정화 및 캘린더 드래그앤드롭 UI 개선 완료**

팀 Calio의 2026년 02월 01일 개발 일지를 보고합니다.

오늘은 프로젝트의 인프라스트럭처 자동화와 핵심 프론트엔드 사용자 경험 개선이라는 두 가지 중요한 영역에서 집중적인 작업이 이루어졌습니다. 인프라 측면에서는 GitHub Actions를 활용한 일일 개발 일지 자동 생성 워크플로를 안정화하는 데 성공했습니다. 특히, 토큰 권한 문제 해결과 한국 시간(KST) 기반의 정확한 날짜 환경 변수 설정이 주요 성과였습니다. 프론트엔드에서는 캘린더 이벤트의 드래그 앤 드롭 기능과 관련된 스타일을 대대적으로 수정하고, 모달 UI의 일관성을 높이는 작업을 완료하여 사용자 경험을 크게 향상시켰습니다.

---

## 🌿 Branch: auto-daily-log

이 브랜치에서는 일일 개발 일지 자동화 워크플로의 초기 설정 및 안정화 작업이 진행되었습니다.

### [yeonjin719] docs: 📝 [Calio] 개발 일지 생성

초기 개발 일지 파일 구조를 설정했습니다.

### [Yeonjin Kim] fix: 토큰 변경

GitHub Actions 내에서 PR을 생성하기 위해 사용되는 토큰을 `secrets.GH_TOKEN`에서 표준 `secrets.GITHUB_TOKEN`으로 변경했습니다. 이는 권한 관리의 일관성을 높이고, 불필요한 Personal Access Token 사용을 줄이는 데 기여합니다.

```diff
  @@ -29,15 +29,15 @@ jobs:
 
       - name: Run Generator
         env:
-          GH_TOKEN: ${{ secrets.GH_TOKEN }}
+          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
           GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
           REPO_NAME: ${{ github.repository }}
         run: python scripts/generate_article.py
 
       - name: Create Pull Request
         uses: peter-evans/create-pull-request@v6
         with:
-          token: ${{ secrets.GH_TOKEN }}
+          token: ${{ secrets.GITHUB_T
```

### [Yeonjin Kim] fix: 제미나이 모델 변경

AI 일지 생성에 사용되는 Gemini 모델을 `gemini-1.5-flash`에서 최신 모델인 `gemini-flash-latest`로 업데이트하여 성능 및 응답 품질을 개선했습니다.

```diff
  @@ -76,7 +76,7 @@ def create_article(work_details):
     today_str = datetime.now(kst).strftime('%Y년 %m월 %d일')
 
     # 모델명 명시화 (Copilot 피드백: gemini-1.5-flash 권장)
-    model = genai.GenerativeModel('gemini-1.5-flash')
+    model = genai.GenerativeModel('gemini-flash-latest')
     
     prompt = f"""
     팀 "Calio"의 시니어 개발자로서 오늘({today_str})의 개발 일지를 작성하라.
```

### [Yeonjin Kim] feat: workflow PR 올리는 형태로 변경

자동 생성된 일지를 메인 브랜치에 즉시 커밋하는 대신, Pull Request를 생성하도록 워크플로를 수정했습니다. 이를 위해 `pull-requests: write` 권한을 추가하고 작업 이름을 `generate-and-pr`로 명시적으로 변경했습니다.

```diff
  @@ -2,20 +2,21 @@ name: Calio Daily Article Generator
 
 on:
   schedule:
-    - cron: "0 15 * * *"
-  workflow_dispatch:
+    - cron: "0 15 * * *" # 한국 시간 기준 매일 자정 (UTC 15:00)
+  workflow_dispatch: # 수동 실행 버튼
 
 jobs:
-  build:
+  generate-and-pr:
     runs-on: ubuntu-latest
     permissions:
       contents: write
+      pull-requests: write # PR을 생성하기 위해 꼭 필요한 권한
 
     steps:
       - name: Checkout Code
         uses: actions/checkout@v4
         with:
-          fetch-depth: 0
+          f
```

### [Yeonjin Kim] Merge pull request #15... & fix: 코드 리뷰 반영 & feat: 워크플로 추가

초기 워크플로 스크립트 병합 및 코드 리뷰 반영이 이루어졌습니다. Python 스크립트 내에서 필수 환경 변수(GH_TOKEN, GEMINI_API_KEY) 검증 로직을 추가하여 안정성을 높였습니다.

## 🌿 Branch: copilot/fix-article-date-issue

### [copilot-swe-agent[bot]] fix: correct date formatting in Calio Daily Article Generator workflow

GitHub Actions 환경에서 KST(한국 표준시) 기반의 정확한 날짜를 가져오지 못하는 문제를 해결하기 위해, `TZ='Asia/Seoul'`을 사용하여 환경 변수를 설정하는 스텝을 추가했습니다. 이는 PR 생성 시 파일명과 제목에 정확한 날짜가 반영되도록 보장합니다.

```diff
  @@ -34,20 +34,25 @@ jobs:
           REPO_NAME: ${{ github.repository }}
         run: python scripts/generate_article.py
 
+      - name: Set Date Environment Variable
+        run: |
+          echo "CURRENT_DATE=$(TZ='Asia/Seoul' date +'%Y-%m-%d')" >> $GITHUB_ENV
+          echo "CURRENT_DATETIME=$(TZ='Asia/Seoul' date +'%Y-%m-%d %H:%M KST')" >> $GITHUB_ENV
+
       - name: Create Pull Request
         uses: peter-evans/create-pull-request@v6
         with:
           token: ${{ secrets.GITHU
```

## 🌿 Branch: feature/#12-이벤트-드래그드롭-변경된-스타일-반영

이 브랜치에서는 캘린더 이벤트, 특히 주간/일간 뷰에서의 드래그 앤 드롭 기능과 관련된 UI/UX 개선이 중점적으로 진행되었습니다.

### [Yeonjin Kim] docs: instructions 수정

Copilot 리뷰 지침 파일에 `applyTo: '**'` 메타데이터를 추가하여, 저장소 전체에 지침이 적용되도록 설정했습니다.

```diff
  @@ -1,3 +1,7 @@
+---
+applyTo: '**'
+---
+
 # 아키텍처·구조 중심 Copilot 리뷰 안내
```

### [Yeonjin Kim] style: 변경된 스타일 반영

모달 및 폼 컴포넌트 전반에 걸쳐 스타일 일관성을 확보했습니다.

*   **`RepeatPanel.style.ts`**: 그리드 간격을 `4px`에서 `6px`로 조정하여 가독성을 높였습니다.
*   **`AddModalLayout.tsx`**: 삭제(Trash) 아이콘의 색상을 명시적으로 `#757575`로 지정하여 시각적 계층을 조정했습니다.
*   **`AddScheduleForm`**: 종일(All-day) 이벤트 여부(`isAllday`)에 따라 날짜/시간 선택 필드의 레이아웃(`flex-direction`)을 동적으로 변경하도록 수정하여, 종일 이벤트 선택 시 UI가 더욱 간결해지도록 개선했습니다.
*   **`EditConfirmModal`**: 이벤트 수정 확인 모달의 버튼 스타일을 변경했습니다. 기존의 빨간색 테두리 대신, `primary2` 배경색과 흰색 텍스트를 사용하여 '적용' 버튼의 시각적 강조를 통일하고, 텍스트를 "이벤트 수정"에서 "적용"으로 변경하여 사용자가 수행하는 행위를 명확히 했습니다.

```diff
// src/shared/ui/modal/EditConfirmModal/EditConfirmModal.style.ts
  @@ -124,12 +124,12 @@ export const CancelButton = styled.button`
 `
 
 export const EditButton = styled.button`
-  border: 1px solid ${theme.colors.red};
   border-radius: 16px;
-  color: ${theme.colors.red};
+  background-color: ${theme.colors.primary2};
   font-size: 16px;
   padding: 12px 20px;
   font-weight: 600;
+  color: ${theme.colors.white};
   cursor: pointer;
   ${media.down(theme.breakPoints.tablet)} {
     font-size: 12px;
```

### [Yeonjin Kim] style: 이벤트 드래그앤드롭 스타일 수정

캘린더의 핵심 기능인 이벤트 상호작용 관련 로직 및 스타일을 개선했습니다.

*   **`WeekEventContainer` 스타일 확장**: 주간 뷰 이벤트 컨테이너에 `pointColor`와 `isSelected` 속성을 추가하여, 이벤트 선택 또는 드래그 중일 때 시각적 피드백을 제공할 수 있도록 기반을 마련했습니다.
*   **`weekView.ts`**: 주간 뷰의 이벤트 간격(`gap`)을 `10px`에서 `6px`로 줄여 밀집도를 높였습니다.
*   **`CalendarEvent` 타입 변경**: 이벤트 ID 타입을 `number`에서 `number | string`으로 확장했습니다. 이는 드래그 앤 드롭 과정에서 임시로 문자열 ID가 할당될 수 있는 상황에 대비하기 위함입니다.
*   **`useStoredCalendarView` 훅 추가**: 사용자가 마지막으로 선택한 캘린더 뷰(월, 주, 일)를 로컬 스토리지에 저장하고 불러오는 훅을 구현하여, 페이지를 새로고침해도 뷰 설정이 유지되도록 UX를 개선했습니다.
*   **모달 컴포넌트 리팩토링**: `CalendarModals.tsx`를 분리하여 모달 렌더링 로직을 중앙 집중화하고, `useCalendarModal` 훅의 `handleAddEvent`에 `eventId` 인자를 추가하여 특정 이벤트 컨텍스트에서 모달을 열 수 있도록 유연성을 확보했습니다.

```diff
// src/features/Calendar/components/CustomEvent/CustomEvent.style.ts
  @@ -68,11 +68,15 @@ export const MonthEventContainer = styled.div<{ backgroundColor?: string }>`
   height: 16px;
 `
 
-export const WeekEventContainer = styled.div<{ backgroundColor?: string }>`
+export const WeekEventContainer = styled.div<{
+  backgroundColor?: string
+  pointColor: string
+  isSelected?: boolean
+}>`
   display: flex;
   flex-direction: column;
   height: fit-content;
```