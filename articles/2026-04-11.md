# [2026년 04월 11일] 일정·할일 전환 동기화 로직 최적화 및 메인 브랜치 배포

오늘 팀 "Calio"는 캘린더 서비스의 사용자 경험(UX)을 결정짓는 핵심 요소인 '일정'과 '할일' 간의 상태 동기화 문제를 해결하는 데 집중했습니다. 사용자가 입력 도중 모드(일정/할일)를 전환하더라도 데이터가 유실되지 않고 미리보기와 일치하도록 로직을 강화했으며, 검증된 코드를 메인 브랜치에 성공적으로 병합하여 서비스 안정성을 확보했습니다.

---

## 🌿 Branch: develop

### [Yujin Ji] 일정·할일 전환 시 입력 상태와 미리보기 동기화 오류 수정

Yujin Ji 님은 사용자가 일정과 할일을 전환할 때 발생하던 입력 상태 불일치 문제를 해결하기 위해 전반적인 폼 관리 로직과 유틸리티를 개선했습니다. 특히 `ItemEditorDraft` 타입을 도입하고 이를 처리하기 위한 전용 유틸리티 함수를 구축하여 데이터의 일관성을 높였습니다.

**1. 모달 상태 관리 및 데이터 빌더 도입**
`CalendarModals.tsx`에서 상태 관리를 위한 `useState`를 추가하고, 새롭게 정의된 `buildDefaultItem`을 통해 모드 전환 시 초기값을 안정적으로 생성하도록 변경되었습니다.

```typescript
// src/features/Calendar/components/CustomCalendar/CalendarModals.tsx
import moment from 'moment'
-import { useMemo } from 'react'
+import { useMemo, useState } from 'react'
 
 import { useDetailEventQuery } from '@/shared/hooks/query/useCalendarQueries'
 import type { CalendarEvent } from '@/shared/types/calendar/types'
+import type { ItemEditorDraft } from '@/shared/types/modal/itemEditor'
 import ScheduleEditorModal from '@/shared/ui/Modals/ScheduleEditor'
 import TodoEditorModal from '@/shared/ui/Modals/TodoEditor'
+import { buildDefaultItem }
```

**2. 폼 필드 훅 최적화**
`useScheduleFormFields`와 `useTodoFormFields`에서 `useRef`를 도입하여 렌더링 사이클 간의 참조를 안정화하고, `draftValues`와의 의존성 관계를 명확히 하여 입력 중인 데이터가 유실되지 않도록 보정했습니다.

```typescript
// src/shared/hooks/form/useScheduleFormFields.ts
-import { useEffect, useMemo } from 'react'
+import { useEffect, useMemo, useRef } from 'react'

// src/shared/hooks/form/useTodoFormFields.ts
-import { useEffect, useMemo } from 'react'
+import { useEffect, useMemo, useRef } from 'react'
```

**3. UI 미세 조정**
이벤트 타이틀의 너비를 조정하여 텍스트 오버플로우 처리가 시각적으로 더 자연스럽게 보이도록 수정했습니다.

```typescript
// src/features/Calendar/components/CustomEvent/CustomEvent.style.ts
 export const EventTitle = styled.div`
   font-weight: 400;
   font-size: 12px;
-  width: 45px;
+  width: 38px;
   white-space: nowrap;
   overflow: hidden;
   text-overflow: ellipsis;
```

**4. Draft 관리 유틸리티 신설**
날짜 포맷팅 및 기본 아이템 객체 생성을 전담하는 `itemEditorDraft.ts`를 추가하여 비즈니스 로직을 분리했습니다.

```typescript
// src/shared/utils/itemEditorDraft.ts
+import type { CalendarEvent } from '@/shared/types/calendar/types'
+import type { RepeatConfigSchema } from '@/shared/types/event/event'
+import type { ItemEditorDraft } from '@/shared/types/modal/itemEditor'
+import { defaultRepeatConfig } from '@/shared/types/recurrence/repeat'
+
+const pad2 = (value: number) => String(value).padStart(2, '0')
+const formatTimeFromDate = (value: Date) => `${pad2(value.getHours())}:${pad2(value.getMinutes())}`
+
+export const buildDefaultItem = ...
```

---

## 🌿 Branch: main

### [Yeonjin Kim] Merge pull request #94 from 2026-Capstone-Project/develop

Yeonjin Kim 님은 `develop` 브랜치에서 검증된 수정 사항들을 `main` 브랜치로 병합했습니다. 이번 배포를 통해 사용자들은 일정과 할일을 자유롭게 오가며 편집하더라도 데이터 동기화 오류 없이 쾌적하게 캘린더 기능을 이용할 수 있게 되었습니다.

**주요 병합 내용:**
- 일정/할일 전환 시 입력 데이터 유지 로직 반영
- 캘린더 이벤트 UI 레이아웃 최적화
- 폼 핸들링 성능 개선을 위한 내부 훅 구조 변경
- 공통 유틸리티(itemEditorDraft) 통합

```diff
// Merge Pull Request #94: develop -> main
// 주요 변경 파일:
// - src/features/Calendar/components/CustomCalendar/CalendarModals.tsx
// - src/features/Calendar/components/CustomEvent/CustomEvent.style.ts
// - src/shared/hooks/form/useScheduleFormFields.ts
// - src/shared/hooks/form/useTodoFormFields.ts
// - src/shared/utils/itemEditorDraft.ts
```

오늘의 작업으로 Calio의 핵심 기능인 캘린더 편집기의 안정성이 한층 강화되었습니다. 수고하셨습니다.