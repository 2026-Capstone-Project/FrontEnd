# [2026년 06월 02일] 일정 에디터 폼 라이프사이클 최적화 및 읽기 전용 모드 UX 개선

오늘 우리 "Calio" 팀은 일정 에디터의 안정성을 높이고 사용자 경험(UX)을 세밀하게 다듬는 작업을 진행했습니다. 메인 에디터 폼의 라이프사이클에서 발생할 수 있는 필드 중복 등록 문제를 해결하였고, 읽기 전용 모드에서의 불필요한 이벤트 트리거를 제한하여 완성도를 높였습니다. 또한, 미사용 상수를 정리하여 코드 베이스를 보다 깔끔하게 유지할 수 있도록 개선했습니다.

---

## 🌿 Branch: develop

### [Yeonjin Kim] Merge pull request #109 from 2026-Capstone-Project/feature/#98

**Yeonjin Kim** 팀원이 작업한 PR #109가 `develop` 브랜치에 병합되었습니다. 이번 작업은 일정 에디터 내의 수정 상태 관리 방식과 제목 입력창의 동작을 직관적으로 개선하는 데 초점을 맞추었습니다. 상세 변경 내역에 대한 기술적 분석은 다음과 같습니다.

#### 1. 중복 상수 제거를 통한 코드 정리
`RECURRENCE_EVENT_SCOPE` 내에서 사용되지 않거나 중복 정의되었던 `THIS_AND_FOLLOWING` 상수를 제거했습니다. 이를 통해 반복 일정 범위를 다루는 데이터 타입의 일관성을 확보했습니다.

```typescript
// src/shared/constants/recurrenceScope.ts
export const RECURRENCE_EVENT_SCOPE = {
  THIS_EVENT: 'THIS_EVENT',
  THIS_AND_FOLLOWING_EVENTS: 'THIS_AND_FOLLOWING_EVENTS',
-  THIS_AND_FOLLOWING: 'THIS_AND_FOLLOWING',
} as const
```

#### 2. 사용자 수정 상태 감지 콜백 추가
일정 에디터 하단 영역(`useScheduleFooter`)과 메인 에디터 컨텐츠(`ScheduleEditorContent`) 간의 상호작용을 강화하기 위해 `onUserEdit` 콜백을 도입했습니다. 사용자가 실제로 수정을 시도할 때 `markUserEdited` 함수가 호출되도록 연결하여, 에디터 폼의 변경 여부(Dirty State)를 보다 정밀하게 추적할 수 있게 되었습니다.

```typescript
// src/shared/hooks/addSchedule/useScheduleFooter.tsx
type UseScheduleFooterProps = {
  occurrenceDate: string
  canEdit?: boolean
  onReadOnlyAttempt?: () => void
+  onUserEdit?: () => void
}

export const useScheduleFooter = ({
  ...
  occurrenceDate,
  canEdit = true,
  onReadOnlyAttempt,
+  onUserEdit,
}: UseScheduleFooterProps) => {
  ...
```

```typescript
// src/shared/ui/Modals/ScheduleEditor/ScheduleEditorContent.tsx
const ScheduleEditorContent = ({
  ...
  occurrenceDate: initialEvent?.occurrenceDate ?? date,
  canEdit,
  onReadOnlyAttempt: showReadOnlyToast,
+  onUserEdit: markUserEdited,
})
```

#### 3. 폼 라이프사이클 내 필드 중복 등록 방지 (성능 최적화)
`useEditorFormLifecycle` 훅에서 `registeredFields`를 순회하며 React Hook Form의 `register`를 호출할 때, 기존에는 매번 무조건적인 등록이 일어날 위험이 있었습니다. 이를 방지하기 위해 `useRef`를 활용한 `registeredFieldSetRef`(`Set` 객체)를 도입했습니다. 이미 등록된 필드는 스킵하도록 분기 처리하여 불필요한 재등록 및 렌더링 낭비를 방지했습니다.

```typescript
// src/shared/hooks/form/useEditorFormLifecycle.ts
export const useEditorFormLifecycle = <TValues extends FieldValues>({
  mapDraft,
}: UseEditorFormLifecycleArgs<TValues>) => {
  const previousResetKeyRef = useRef(resetKey)
+  const registeredFieldSetRef = useRef(new Set<Path<TValues>>())
  const { register, reset } = formMethods

  useEffect(() => {
-    registeredFields.forEach((field) => register(field))
+    registeredFields.forEach((field) => {
+      if (registeredFieldSetRef.current.has(field)) return
+      registeredFieldSetRef.current.add(field)
+      register(field)
+    })
  ...
```

#### 4. 읽기 전용 모드에서의 키보드 이벤트 핸들링 개선
기존 `TitleSuggestionInput` 컴포넌트에서는 읽기 전용(`readOnly`) 상태일 때 사용자가 아무 키나 입력해도 경고 토스트(`onReadOnlyAttempt`)가 발생하고 기본 동작이 막히는 문제가 있었습니다. 이는 사용자가 단순히 방향키를 누르거나 텍스트를 드래그/복사하려 할 때도 경고가 뜨는 부자연스러운 UX를 유발했습니다. 

이를 개선하여 오직 'Enter' 키를 눌렀을 때만 폼 제출 시도를 차단하고 경고 토스트를 띄우도록 이벤트를 제한했습니다.

```typescript
// src/shared/ui/scheduleTodo/TitleSuggestionInput/TitleSuggestionInput.tsx
const handleKeyDown = (event: React.KeyboardEvent<HTMLInputElement>) => {
  if (readOnly) {
-    event.preventDefault()
-    onReadOnlyAttempt?.()
+    if (event.key === 'Enter') {
+      event.preventDefault()
+      onReadOnlyAttempt?.()
+    }
    return
  }
  if (event.key !== 'Enter') return
```

---

### 💡 시니어 개발자 총평
이번 작업은 대규모 기능 추가는 아니지만, 실제 프로덕션 환경에서 사용자가 느낄 수 있는 미세한 불편함(UX)과 React Hook Form의 렌더링 효율성(Performance)을 동시에 잡아낸 훌륭한 리팩토링입니다. 특히 `Set` 자료구조를 활용해 폼 필드의 중복 등록을 막은 설계와 읽기 전용 상태에서의 키보드 인터랙션 세분화는 프론트엔드 완성도를 한 단계 끌어올렸습니다. 수고하셨습니다.